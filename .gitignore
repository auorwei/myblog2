# .env # ALL .env files - 注释掉，因为测试环境需要包含配置文件
# .env.* - 注释掉，因为测试环境需要包含配置文件
############################
# OS X
############################

.DS_Store
.AppleDouble
.LSOverride
Icon
.Spotlight-V100
.Trashes
._*


############################
# Linux
############################

*~


############################
# Windows
############################

Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.cab
*.msi
*.msm
*.msp


############################
# Packages
############################

*.7z
*.csv
*.dat
*.dmg
*.gz
*.iso
*.jar
*.rar
*.tar
*.zip
*.com
*.class
*.dll
*.exe
*.o
*.seed
*.so
*.swo
*.swp
*.swn
*.swm
*.out
*.pid


############################
# Logs and databases
############################

# .tmp/ - 注释掉，因为需要包含数据库文件
*.log
*.sql
*.sqlite
*.sqlite3
# database/data.db # Strapi's default SQLite file location if used locally - 注释掉，允许包含数据库


############################
# Misc.
############################

*#
# ssl/ # If you store SSL certs locally for dev, exclude them
.idea/
nbproject/
.tsbuildinfo
.eslintcache

############################
# Node.js
############################

lib-cov/
lcov.info
pids/
logs/
results/
node_modules/
.node_history

############################
# Package managers
############################

.yarn/*
!.yarn/cache
!.yarn/unplugged
!.yarn/patches
!.yarn/releases
!.yarn/sdks
!.yarn/versions
.pnp.*
yarn-error.log
npm-debug.log*
yarn-debug.log*

############################
# Tests
############################

coverage/
.nyc_output/

############################
# Strapi
############################

# .env # ALL .env files - 注释掉，测试环境需要包含
# .env.* - 注释掉，测试环境需要包含
!.env.example # Keep example files
!.env.development.example
!.env.production.example

license.txt
exports/
.strapi/ # Strapi cache and auto-generated files
dist/ # Build output
build/ # Common build output folder

# Public uploads - generally, you might want to back these up separately
# or use a dedicated storage service (S3, Cloudinary) in production.
# If you commit them, be mindful of repository size.
# For now, we assume they are managed via Docker volume and backed up separately.
public/uploads/*
!public/uploads/.gitkeep

# Strapi Cloud specific (if not using, can be removed)
.strapi-updater.json
.strapi-cloud.json

# Docker specific
Dockerfile
docker-compose.yml
.dockerignore
# deploy/env.production.example # Already covered by !.env.production.example if you follow that pattern

# Deployment scripts with sensitive info (adjust as needed)
deploy/.env*
deploy/quick-deploy.ps1

# Backup files that might be created by scripts
*.sql # Database backups, e.g., from deploy.sh
*.tar.gz
*.zip

# VSCode
.vscode/

#