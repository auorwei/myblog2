# 生产环境配置示例 - 复制为 .env.production 并填入真实值
NODE_ENV=production
HOST=0.0.0.0
PORT=1337

# 应用密钥 - 请更改为随机字符串，例如：
# openssl rand -base64 32 (生成一个)
# APP_KEYS=your-app-key-1,your-app-key-2,your-app-key-3,your-app-key-4
APP_KEYS=

# 数据库配置（PostgreSQL）
DATABASE_CLIENT=postgres
DATABASE_HOST=strapi-db # docker-compose 中的服务名
DATABASE_PORT=5432
DATABASE_NAME=strapi
DATABASE_USERNAME=strapi
DATABASE_PASSWORD=your_very_secure_password # 请务必修改此密码
DATABASE_SSL=false

# JWT 密钥 - 请更改为随机字符串
# openssl rand -base64 32 (生成一个)
JWT_SECRET=

# Admin JWT 密钥 - 请更改为随机字符串
# openssl rand -base64 32 (生成一个)
ADMIN_JWT_SECRET=

# API Token Salt - 请更改为随机字符串
# openssl rand -base64 32 (生成一个)
API_TOKEN_SALT=

# Transfer Token Salt - 请更改为随机字符串
# openssl rand -base64 32 (生成一个)
TRANSFER_TOKEN_SALT=

# 可选：服务器公开 URL (如果通过反向代理访问)
# PUBLIC_URL=https://your-domain.com
# STRAPI_ADMIN_BACKEND_URL=https://your-domain.com/admin # 如果管理后台有特定路径 